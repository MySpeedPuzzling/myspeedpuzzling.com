<div class="row">
    <div class="col-md-6 col-lg-4 my-2">
        <div class="card card-body shadow p-0">
            <div class="statistics-puzzle-solved-placeholder d-flex justify-content-between align-items-center">
                <div class="w-100 text-center">
                    <button
                        class="btn btn-outline-secondary bg-white btn-sm px-3 fs-sm"
                        data-bs-toggle="modal"
                        data-bs-target="#membersExclusiveModal"
                    >
                        <i class="ci-locked me-1"></i>
                        {{ 'membership.members_exclusive_button'|trans }}
                    </button>
                </div>
            </div>
        </div>
    </div>

    <div class="col-md-6 col-lg-4 my-2">
        <div class="card card-body shadow p-0">
            <div class="statistics-time-spent-placeholder d-flex justify-content-between align-items-center">
                <div class="w-100 text-center">
                    <button
                        class="btn btn-outline-secondary bg-white btn-sm px-3 fs-sm"
                        data-bs-toggle="modal"
                        data-bs-target="#membersExclusiveModal"
                    >
                        <i class="ci-locked me-1"></i>
                        {{ 'membership.members_exclusive_button'|trans }}
                    </button>
                </div>
            </div>
        </div>
    </div>

    <div class="col-md-6 col-lg-4 my-2">
        <div class="card card-body shadow p-0">
            <div class="statistics-total-pieces-placeholder d-flex justify-content-between align-items-center">
                <div class="w-100 text-center">
                    <button
                        class="btn btn-outline-secondary bg-white btn-sm px-3 fs-sm"
                        data-bs-toggle="modal"
                        data-bs-target="#membersExclusiveModal"
                    >
                        <i class="ci-locked me-1"></i>
                        {{ 'membership.members_exclusive_button'|trans }}
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="row mt-4">
    <div class="col-md-6 col-lg-4 my-2">
        <div class="card card-body shadow p-0">
            <div class="statistics-pieces-1-placeholder d-flex justify-content-between align-items-center">
                <div class="w-100 text-center">
                    <button
                        class="btn btn-outline-secondary bg-white btn-sm px-3 fs-sm"
                        data-bs-toggle="modal"
                        data-bs-target="#membersExclusiveModal"
                    >
                        <i class="ci-locked me-1"></i>
                        {{ 'membership.members_exclusive_button'|trans }}
                    </button>
                </div>
            </div>
        </div>
    </div>

    <div class="col-md-6 col-lg-4 my-2">
        <div class="card card-body shadow p-0">
            <div class="statistics-pieces-2-placeholder d-flex justify-content-between align-items-center">
                <div class="w-100 text-center">
                    <button
                        class="btn btn-outline-secondary bg-white btn-sm px-3 fs-sm"
                        data-bs-toggle="modal"
                        data-bs-target="#membersExclusiveModal"
                    >
                        <i class="ci-locked me-1"></i>
                        {{ 'membership.members_exclusive_button'|trans }}
                    </button>
                </div>
            </div>
        </div>
    </div>

    <div class="col-md-6 col-lg-4 my-2">
        <div class="card card-body shadow p-0">
            <div class="statistics-pieces-3-placeholder d-flex justify-content-between align-items-center">
                <div class="w-100 text-center">
                    <button
                        class="btn btn-outline-secondary bg-white btn-sm px-3 fs-sm"
                        data-bs-toggle="modal"
                        data-bs-target="#membersExclusiveModal"
                    >
                        <i class="ci-locked me-1"></i>
                        {{ 'membership.members_exclusive_button'|trans }}
                    </button>
                </div>
            </div>
        </div>
    </div>

    <div class="col-md-6 col-lg-4 my-2">
        <div class="card card-body shadow p-0">
            <div class="statistics-pieces-4-placeholder d-flex justify-content-between align-items-center">
                <div class="w-100 text-center">
                    <button
                        class="btn btn-outline-secondary bg-white btn-sm px-3 fs-sm"
                        data-bs-toggle="modal"
                        data-bs-target="#membersExclusiveModal"
                    >
                        <i class="ci-locked me-1"></i>
                        {{ 'membership.members_exclusive_button'|trans }}
                    </button>
                </div>
            </div>
        </div>
    </div>

</div>
