{% extends 'base.html.twig' %}

{% block title %}{{ 'collections.edit_system.meta.title'|trans }}{% endblock %}

{% block content %}
    <twig:PlayerHeader player="{{ player }}" />

    {% include '_return_back_button.html.twig' %}

    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-lg">
                <div class="card-header">
                    <h3 class="mb-0">{{ 'collections.edit_system.title'|trans }}</h3>
                </div>
                <div class="card-body">
                    {{ form_start(form) }}

                    <div class="mb-4">
                        {{ form_row(form.visibility) }}
                        <small class="form-text text-muted">
                            {{ 'collections.form.visibility_help'|trans }}
                        </small>
                    </div>
                    
                    <div class="d-flex justify-content-between">
                        <a href="{{ cancelUrl }}" class="btn btn-secondary">
                            {{ 'forms.cancel'|trans }}
                        </a>
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-check-lg me-1"></i>{{ 'collections.edit_system.submit'|trans }}
                        </button>
                    </div>
                    
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
