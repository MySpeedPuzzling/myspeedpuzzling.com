{% if computed.chart.data['labels']|length > 3 %}
    <div class="card card-body p-2 my-3 shadow">
        <div style="height: 200px;">
            {{ render_chart(computed.chart, {'data-controller': 'time-chart'}) }}
        </div>
    </div>
{% endif %}
