{% apply inline_css(source('@styles/email.css')) %}
    <html>
    <body>

    <div class="newsletterContainer">
        <div class="text-center">
            <img src="{{ email.image('@images/speedpuzzling-logo.png') }}" class="logo" alt="Logo">

            <h1 class="newsletter">Your subscription has been cancelled</h1>

            <p>Sorry to see you go, maybe, we will see you back, one day :-).</p>
            <p>The membership will remain active until end of the billing period: <strong>{{ expiresAt }}</strong></p>

            <p>
                You can always manage your membership on the <a href="{{ url('membership') }}">membership page</a>.
            </p>
        </div>
    </div>

    </body>
    </html>
{% endapply %}
