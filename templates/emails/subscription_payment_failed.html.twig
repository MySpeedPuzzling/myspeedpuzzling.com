{% apply inline_css(source('@styles/email.css')) %}
    <html>
    <body>

    <div class="newsletterContainer">
        <div class="text-center">
            <img src="{{ email.image('@images/speedpuzzling-logo.png') }}" class="logo" alt="Logo">

            <h1 class="newsletter">Subscription payment have failed</h1>

            <p>Something unexpected happened, please check your payment method on the <a href="{{ url('billing_portal') }}">billing portal page</a>.</p>
        </div>
    </div>

    </body>
    </html>
{% endapply %}
