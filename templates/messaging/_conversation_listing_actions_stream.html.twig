{% if close_modal|default(false) %}
    {{ include('_modal_close_stream.html.twig') }}
{% endif %}

<turbo-stream action="update" target="conversation-listing-actions-{{ puzzle_context.itemId }}">
    <template>
        {{ include('messaging/_conversation_listing_actions.html.twig') }}
    </template>
</turbo-stream>

{{ include('_toast_stream.html.twig', {message: message}) }}
