{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block content %}
    <h1>Profil</h1>

    <h2 class="h4">
        {{ player.playerName }}
    </h2>

    <p>{{ player.city }} {{ player.country }}</p>

    <div class="table-responsive shadow mt-3 mb-5">
        <table class="table table-striped table-hover mb-0">
            <thead>
            <tr>
                <th>Puzzle</th>
                <th></th>
                <th class="text-end">Čas</th>
            </tr>
            </thead>
            <tbody>
            {% for puzzle in puzzles %}
                <tr>
                    <td>
                        <a href="{{ path('puzzle_detail', {puzzleId: puzzle.puzzleId}) }}">
                            {{ puzzle.puzzleName }} ({{ puzzle.piecesCount }} dílků)
                        </a>
                    </td>
                    <td>{{ puzzle.groupName }}</td>
                    <td class="text-end">{{ puzzle.time|date('H:i:s', 'UTC') }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
