{% extends 'base.html.twig' %}

{% set is_ap = tab == 'achievement-points' %}

{% block title %}{{ (is_ap ? 'xp.leaderboard.ap_title' : 'xp.leaderboard.meta_title')|trans }}{% endblock %}

{% block content %}
    <header class="mb-3">
        <h1 class="h3 mb-1">
            <i class="bi bi-trophy me-1"></i>
            {{ (is_ap ? 'xp.leaderboard.ap_title' : 'xp.leaderboard.title')|trans }}
        </h1>
        <p class="text-body-secondary fs-sm mb-0">
            {{ (is_ap ? 'xp.leaderboard.ap_subtitle' : 'xp.leaderboard.subtitle')|trans }}
            <a href="{{ path('xp_explainer') }}">{{ 'badges.how_it_works_link'|trans }}</a>
            <span class="mx-1">·</span>
            <a href="{{ path('ladder') }}">{{ 'xp.leaderboard.all_leaderboards'|trans }}</a>
        </p>
    </header>

    {# Two disciplines, deliberately separate boards - chase XP, chase AP, or both. #}
    <ul class="nav nav-tabs mb-3">
        {% for tab_option in ['xp', 'achievement-points'] %}
            <li class="nav-item">
                <a class="nav-link {{ tab == tab_option ? 'active' }}"
                   href="{{ path('xp_leaderboard', {tab: tab_option, country: selected_country, favorites: favorites_only ? 1 : null}) }}">
                    {{ ('xp.leaderboard.tab.' ~ tab_option)|trans }}
                </a>
            </li>
        {% endfor %}
    </ul>

    <form method="get" class="d-flex flex-wrap align-items-center gap-3 mb-3" data-turbo="false">
        <input type="hidden" name="tab" value="{{ tab }}">

        <div class="d-flex align-items-center gap-2">
            <label for="board-country" class="form-label fs-sm mb-0 text-nowrap">{{ 'badges.detail.country_filter'|trans }}</label>
            <select id="board-country" name="country" class="form-select form-select-sm" onchange="this.form.submit()">
                <option value="">{{ 'badges.detail.all_countries'|trans }}</option>
                {% for country in countries %}
                    <option value="{{ country }}" {{ selected_country == country ? 'selected' }}>{{ country|upper }}</option>
                {% endfor %}
            </select>
        </div>

        {% if viewer is not null %}
            <div class="form-check form-switch fs-sm">
                <input class="form-check-input" type="checkbox" id="favorites-only" name="favorites" value="1"
                       {{ favorites_only ? 'checked' }} onchange="this.form.submit()">
                <label class="form-check-label" for="favorites-only">{{ 'xp.leaderboard.favorites_only'|trans }}</label>
            </div>
        {% endif %}
    </form>

    {% if is_ap and viewer is null %}
        <div class="xp-teaser">
            <i class="ci-user" aria-hidden="true"></i>
            <span>{{ 'xp.leaderboard.ap_login_required'|trans }}</span>
        </div>
    {% elseif rows|length == 0 %}
        <div class="xp-teaser">
            <i class="ci-star" aria-hidden="true"></i>
            <span>{{ 'xp.leaderboard.empty'|trans }}</span>
        </div>
    {% else %}
        {% if self_rank is not null %}
            <div class="card shadow-sm mb-2 border-primary" id="xp-self-row">
                <div class="card-body py-2 px-3 d-flex align-items-center gap-2 fs-sm">
                    <strong>#{{ self_rank.rank }}</strong>
                    <span>{{ 'xp.leaderboard.your_position'|trans }}</span>
                    <span class="ms-auto xp-receipt-amount">
                        {{ is_ap
                            ? 'xp.leaderboard.ap_value'|trans({'%points%': self_rank.value})
                            : 'xp.leaderboard.xp_value'|trans({'%xp%': self_rank.value}) }}
                    </span>
                </div>
            </div>
        {% endif %}

        <div class="custom-table-wrapper">
            <table class="table table-striped mb-0">
                <thead>
                    <tr class="fs-sm">
                        <th scope="col" style="width: 3.5rem;">#</th>
                        <th scope="col">{{ 'xp.leaderboard.player'|trans }}</th>
                        <th scope="col" class="text-end">{{ ('xp.leaderboard.value.' ~ tab)|trans }}</th>
                    </tr>
                </thead>
                <tbody>
                    {% for row in rows %}
                        <tr class="{{ viewer is not null and row.playerId == viewer.playerId ? 'table-primary' }}">
                            <td class="fs-sm">
                                {% if row.rank == 1 %}🥇{% elseif row.rank == 2 %}🥈{% elseif row.rank == 3 %}🥉{% else %}{{ row.rank }}{% endif %}
                            </td>
                            <td>
                                <a href="{{ path('player_profile', {playerId: row.playerId}) }}" class="d-inline-flex align-items-center gap-2 text-decoration-none">
                                    {% if row.avatar is not null %}
                                        <img src="{{ row.avatar|thumbnail('avatar') }}" alt="" width="24" height="24" class="rounded-circle" style="object-fit: cover;">
                                    {% endif %}
                                    <span>{{ row.playerName ?? ('#' ~ row.playerCode|upper) }}</span>
                                    {% if row.countryCode is not null %}
                                        <span class="fi fi-{{ row.countryCode.name }}"></span>
                                    {% endif %}
                                    <span class="xp-level-chip {{ row.isMaxLevel ? 'xp-level-chip-max' }}">{{ 'xp.level_chip'|trans({'%level%': row.level}) }}</span>
                                </a>
                            </td>
                            <td class="text-end fw-semibold">
                                {{ is_ap
                                    ? 'xp.leaderboard.ap_value'|trans({'%points%': row.value})
                                    : 'xp.leaderboard.xp_value'|trans({'%xp%': row.value}) }}
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    {% endif %}
{% endblock %}
